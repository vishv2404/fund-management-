import 'package:flutter/material.dart';

class AppColors {
  // Primary Background: Light blue/grey from the image
  static const Color primaryBackground = Color(0xFFE8F0F5); // A soft, light blue-grey

  // Accent Green: A softer, pastel green
  static const Color accentGreen = Color(0xFFB3E068); // A more muted, pleasant green

  // Dark Text Color
  static const Color textDark = Color(0xFF303030); // Dark grey for primary text

  // Light Text Color (for buttons etc.)
  static const Color textLight = Color(0xFFFFFFFF); // White

  // Error Red
  static const Color errorRed = Color(0xFFE53935);

  // Success Green (can be the same as accentGreen or slightly different)
  static const Color successGreen = Color(0xFF66BB6A); // A slightly darker green for success messages

  // Hint Grey / Placeholder text
  static const Color hintGrey = Color(0xFF9E9E9E);

  // Border Color
  static const Color borderColor = Color(0xFFCCCCCC);

  // Button Shadow
  static const Color buttonShadow = Color(0x33000000); // Semi-transparent black for shadow

  // Card Background (from image)
  static const Color cardBackground = Color(0xFFFFFFFF); // White for cards
}
